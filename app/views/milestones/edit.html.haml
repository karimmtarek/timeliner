= content_for(:title) do
  = "Edit Milestone - #{@user.first_name} #{@user.last_name} | Timeliner"

.container
  .row.full.page-title.clearfix
    %h2.dark.underline.left.thick.yellow
      Milestone
      %span.heighlight (Edit)

  = render 'form'
